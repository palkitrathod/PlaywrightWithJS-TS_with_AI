# Class and object 
1. Class
2. Object
3. Constructor ( there only one type of constructor in one class)
    1. Default, Default Values
    2. Parameter

4. Private, Public, ~Protected~ (It is not supported JS)
---

**Class** - Blueprint contains **attribute** and **behaviours.**

**Object** -> Real entity, instance of the class.

**Constructor** -> The Setup Function, The `**constructor()**` runs AUTOMATICALLY when you write `new ClassName()`. It sets up the initial values.

The **this Keyword** -> `this` refers to the CURRENT OBJECT — the one calling the method.

